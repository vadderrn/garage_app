name: ui
description: Shared UI widgets and themes for Garage app
version: 0.1.0
publish_to: none
environment:
  sdk: ^3.11.4

dependencies:
  flutter:
    sdk: flutter
  backend:
    path: ../backend
  provider: ^6.1.2
  freezed_annotation: ^3.0.0
  json_annotation: ^4.12.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.4.0
  flutter_lints: ^6.0.0
  freezed: ^3.0.0
  json_serializable: ^6.8.0
